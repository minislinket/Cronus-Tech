Tech App:


INDEXED DB
--->>>> See if we can use indexed DB as a solution to customer stores so we don't have to decompress them so many times.





Call Operations ASAP:
    DONE- Ops Admin Role (101 - local Ops Admin)
    - Call Logging
        - Check for and link Old Calls (at making new call)
        - Check if tech in area (at allocate tech)
    - Allocated Call
        DONE - WARN if no tech




-----------------------------
Cronus 2.1:

- Customer Store Address Management (try finish Customer module)
x- Job Card Draft
- 2.0 to 2.1 token hand-off (chrome update issue?)
- tumbler arrow makes number run (vue issue i think..)